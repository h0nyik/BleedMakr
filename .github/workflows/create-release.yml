name: Create Release (DISABLED)

# Deaktivováno - používá se build-release.yml pro tagy
on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  create-release:
    runs-on: ubuntu-22.04
    
    steps:
    - name: Disabled
      run: echo "This workflow is disabled. Use build-release.yml for tag-based releases." 